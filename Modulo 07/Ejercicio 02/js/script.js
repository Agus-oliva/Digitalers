let usuario = prompt("Por favor, introduce tu nombre de usuario:");
let clave = prompt("Por favor, introduce tu clave:");


if (usuario === "admin" && clave === "1234") {

    alert("¡Bienvenido@a, admin!");
} else {

    alert("Usuario o clave incorrectos. Por favor, inténtalo de nuevo.");
}
